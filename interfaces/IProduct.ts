export interface WebProduct {
  name: string;
  slug: string;
  price: number;
  progLang: string;
  prodType: string;
  description: string;
}

export interface WebProductFeatures {
  title: string;
  description: string;
}
